services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${APP_PORT:-3000}:80"
    env_file:
      - .env
    networks:
      - frontend
    restart: always

networks:
  frontend:
    driver: bridge