Act as an Expert React & Vite Developer. Your objective is to build the frontend architecture and integrate a Laravel API.

1. API INTEGRATION: Read `postman/frontend-api-map.md` as your roadmap. Cross-reference it with `postman/Teamoria API - v1.postman_collection.json` to get exact endpoints, HTTP methods, and payloads.
2. DESIGN BY REFERENCE: We do not have Figma designs. You MUST extract the design system (colors, layout, spacing, typography) strictly from `index.html` and `src/styles.css`. Any new React components you build must perfectly match this existing aesthetic.
3. STANDARDS: Strictly follow the Vercel React Best Practices and Web Design Guidelines provided in the `.agents/skills` files. Write clean, modular, and modern code.
4. ENVIRONMENT VARIABLES (CRITICAL): Whenever you need a dynamic variable (like API Base URLs or Auth Tokens), you must create or update the `.env.example` file and add the keys as empty templates (e.g., VITE_API_ORIGIN=).
5. EXECUTION: Build the API services and integrate them into the React components as outlined in the API map. Handle loading states and errors gracefully.

Please output the variables you are adding to `.env.example` first, then generate the code.